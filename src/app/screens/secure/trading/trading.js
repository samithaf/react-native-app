import React from 'react';
import {ExpandedAssetCard} from '../../../components';

export const Trading = (props) => {
    return (
        <ExpandedAssetCard {...props}/>
    )
};

export default Trading;
